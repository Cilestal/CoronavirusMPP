ext {
    commonDependencyGroup = {
        api kotlinx_serialization
    }

    jvmDependencyGroup = {
        api kotlinx_serialization_jvm
    }
}

apply from: rootProject.file("${rootDir}/config/mpp.gradle")